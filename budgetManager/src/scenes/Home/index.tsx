import React, { useContext } from 'react';

type Props = {};

const Home = (props: Props) => {
  return (
    <div className="flex flex-col gap-16 py-16">
      <h4>HG</h4>
    </div>
  );
};

export default Home;
